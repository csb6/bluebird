function main() is
    1 + 1;
    //true;
    'a';
    let a: Integer;
    a + 3;
    a != 3;
    -a;
    a / 17;

    let b: Boolean := false;
    b or false;
    b and true;
    not b;
    b := true;
end main;

function foo(): Integer is
    return 27;
end;

function void() is
   27;
   let b: Integer := 67;
end;

function with_args(a: Integer, b: Boolean) is
   a := 1 + 2;
   b := true or false;
end;

function if_else() is
  let a: Integer := 99;
  let b: Integer := 66;
  let c: Float := 1.0;
  c := c - 1.0;
  if c = 8.0 - 1.0 do
     c := c * 9.0 / 5.644;
  end;
  if a = b do
    a := 0;
  else if a < b do
    a := 1;
  else
    a := 2;
  end if;
end;

function while_loop() is
  let a: Integer := 78;
  while(a < 72 and a >=0 and a != 9999) loop
    if a = 16 do
      a := 13;
    else
      a := 16;
    end if;
  end while;
end while_loop;
