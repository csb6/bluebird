function main() is
    1 + 1;
    //true;
    'a';
    let a: Integer;
    a + 3;
    a != 3;
    -a;
    a / 17;

    let b: Boolean := false;
    b or false;
    b and true;
    not b;
    b := true;
end main;

function foo(): Integer is
    return 27;
end;

function void() is
   27;
   let b: Integer := 67;
end;

function with_args(a: Integer, b: Boolean) is
   a := 1 + 2;
   b := true or false;
end;

function if_else() is
  let a: Integer := 99;
  let b: Integer := 66;
  if a = b do
    a := 0;
  else if a < b do
    a := 1;
  else
    a := 2;
  end if;
end;
