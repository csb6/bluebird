type Positive is range 0 thru 1_000_000;
type Natural is range 1 thru 1_000_000;
type One_Number is range 89 thru 89;
// type Zero_Number is range 89 upto 89; // Illegal
// type Impossible_Range is range -88 thru -89; //Illegal

// This is a line comment
function foo(a : Natural, b : Integer) is
   let n : constant Number = 45;
   let x : Number = 23;
   x = 4 + 1;
   x = n;
   let p : Integer;
   n - x + (n / 6);
   4 + 5 + (9 - 6);
   let j : Number;
end foo;

type Number is range 5 upto 42;

/*Some comment
function not_really is end not_really

*/
function main() is
    let n : constant Number = 89;
    1 + 1;
    // More comments
    2 * 10 + 4;
    2 * 2 + 4 / 5;
    1 * 1 * (4 + 6);
end main;
